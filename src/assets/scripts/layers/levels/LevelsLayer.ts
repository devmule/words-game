import * as cc from 'cc';

const {ccclass, property, executeInEditMode} = cc._decorator;

@ccclass('LevelsLayer')
@executeInEditMode
export class LevelsLayer extends cc.Component {

    start() {
    }

    update(dt: number) {
    }

}
